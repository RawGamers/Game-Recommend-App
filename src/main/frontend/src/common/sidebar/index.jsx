import styles from "./index.module.scss";

const SideBar = () => {
    return (
        <aside className={styles.container}>
            <p>SideBar</p>
        </aside>
    );
};

export default SideBar;
